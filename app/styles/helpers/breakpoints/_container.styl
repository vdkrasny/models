.container
	margin-x auto
	padding-x ($container-gutter / 2)
	width 100%

if $container-fill
	.container-fill
		@extends .container

for key, value in $breakpoints
	if $container-size[key] != 0
		+media(value)
			.container
				max-width $container-size[key]